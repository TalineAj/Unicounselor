import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.unicounselor.app',
  appName: 'Unicounselor',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
